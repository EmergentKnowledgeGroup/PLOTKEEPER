[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "plotkeeper"
version = "0.1.0"
description = "Read-only Codex Specswarm run ledger and review gate"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["plotkeeper*"]
